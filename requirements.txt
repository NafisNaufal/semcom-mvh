# Python 3.11. Install torch/torchvision matching your CUDA (see pytorch.org).
torch
torchvision
timm==0.6.13
pytorch-msssim
numpy
pillow
tqdm
matplotlib
tensorboardX
pyyaml
# classifier weights (chenyaofo CIFAR-10 ResNet-20) are fetched via torch.hub at runtime.
